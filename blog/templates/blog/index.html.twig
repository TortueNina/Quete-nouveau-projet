{% extends 'base.html.twig' %}

{% block title %}All articles{% endblock %}

{% block body %}
    <h1>Mes articles : </h1>
    {% for article in articles %}
        <div class="container">
            <h2><u>{{ loop.index }} / {{ article.title }}</u></h2>
                <h3>Category : {{ article.category.name }}</h3>
            <p>{{ article.content }}</p>
        </div>
    {% else %}
        Aucun article trouvé.
    {% endfor %}
    <a href="{{ path('app_index') }}">
        Back to homepage.
    </a>
{% endblock %}
